# Serving Static Files in Ktor

A simple example of how to serve static content from the application classpath
in a [Ktor][ktor] web application, using `staticResources()`.

Run the server with

    ./amper run

Then point a browser at `http://0.0.0.0:8080/` to see a simple HTML document.


[ktor]: https://ktor.io/
